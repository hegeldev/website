---
title: Hegel
description: A universal property-based testing protocol and family of libraries, built on Hypothesis
tableOfContents: false
---

import { Card, CardGrid, LinkCard, Tabs, TabItem } from '@astrojs/starlight/components';
import hedgel from '../../assets/hedgel-small.webp';

{/* Starlight renders an empty first .content-panel just to hold the autogenerated
    page title. We hide it entirely (rather than only hiding the h1) so the
    surrounding panel padding doesn't leave a phantom band at the top. */}
<style>{".content-panel:first-of-type { display: none; }"}</style>

<div style="text-align: center; margin-top: 2rem; margin-bottom: 2rem;">
  <img src={hedgel.src} alt="Hedgel mascot" style="width: 200px; margin: 0 auto;" />
  <h1 style="margin-top: 1rem;">Hegel</h1>
</div>

Hegel is a universal property-based testing protocol and family of libraries, built on [Hypothesis](https://github.com/hypothesisworks/hypothesis).

First time here? We recommend starting with the [Getting started](/intro/getting-started) guide, or reading through the example Hegel code below.

{/* note that we intentionally use tabs in these code blocks so we can control their size on mobile vs computer */}

<Tabs syncKey="hegel-lang">
  <TabItem label="Rust">
```rust
use hegel::generators as gs;
use hegel::TestCase;

#[hegel::test]
fn test_respects_lru_capacity(tc: TestCase) {
	let capacity = tc.draw(gs::integers::<usize>().min_value(0));
	let mut cache = MyLRUCache::<String, i64>::new(capacity);

	let entries = tc.draw(
	    gs::vecs(gs::tuples!(gs::text(), gs::integers::<i64>()))
	);
	for (key, value) in entries {
		cache.put(key, value);
	}

	assert!(cache.size() <= capacity);
}
```
  </TabItem>
  <TabItem label="Go">
```go
import (
  "math"
  "testing"
  "hegel.dev/go/hegel"
)

func TestRespectsLRUCapacity(t *testing.T) {
	t.Run("MyLRUCache respects capacity", hegel.Case(func(ht *hegel.T) {
		capacity := hegel.Draw(ht, hegel.Integers(0, math.MaxInt))
		cache := NewMyLRUCache[string, int](capacity)

		keys := hegel.Draw(ht, hegel.Lists(hegel.Text(0, math.MaxInt)))
		for _, key := range keys {
			value := hegel.Draw(ht, hegel.Integers(math.MinInt, math.MaxInt))
			cache.Put(key, value)
		}

		if cache.Size() > capacity {
			ht.Fatalf("cache size exceeds capacity")
		}
	}))
}
```
  </TabItem>
  <TabItem label="C++">
```cpp
#include <hegel/hegel.h>
namespace gs = hegel::generators;

int main() {
	hegel::test([](hegel::TestCase& tc) {
		auto capacity = tc.draw(gs::integers<size_t>({.min_value = 0}));
		MyLRUCache<std::string, int> cache(capacity);

		auto entries = tc.draw(
		  gs::vectors(gs::tuples(gs::text(), gs::integers<int>()))
		);
		for (const auto& [key, value] : entries) {
			cache.put(key, value);
		}

		assert(cache.size() <= capacity);
	});
}
```
  </TabItem>
  <TabItem label="TypeScript">
```typescript
import { test, expect } from "vitest";
import * as hegel from "@hegeldev/hegel";
import * as gs from "@hegeldev/hegel/generators";

test(
	"MyLRUCache respects capacity",
	hegel.test((tc) => {
		const capacity = tc.draw(gs.integers({ minValue: 0 }));
		const cache = new MyLRUCache<string, number>(capacity);

		const entries = tc.draw(
		    gs.arrays(gs.tuples(gs.text(), gs.integers())),
		);
		for (const [key, value] of entries) {
		    cache.put(key, value);
		}

		expect(cache.size()).toBeLessThanOrEqual(capacity);
	}),
);
```
  </TabItem>
</Tabs>

<CardGrid>
  <Card title="Introduction">
   - [Getting started](/intro/getting-started)
  </Card>
  <Card title="How-to guides">
    *coming soon!*
  </Card>
  <Card title="Explanation">
    - [How Hegel works](/explanation/how-hegel-works)
    - [Why Hegel?](/explanation/why-hegel)
  </Card>
  <Card title="Reference">
    - [Installation reference](/reference/installation)
    - [Protocol reference](/reference/protocol)
  </Card>
</CardGrid>
